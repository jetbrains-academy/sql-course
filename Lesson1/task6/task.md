Write a couple of lines in the placeholder so that at the end of the function 
the map `mapRegionToSum` contained all regions except for _Asia_ as keys, and every key was 
mapped to the total sums of movements in the airports from that region.
In this exercise you have to edit a few expressions and make them returning the
expected results. Read the task definitions in `task.sql` file, edit the code below the tasks
and click the button `Check`.

### About the `?` placeholder
This query contains a placeholder written as a question mark: `?`.
While evaluating your solution, the automated checker substitutes different values into this placeholder.
**Keep the `?` in the query** — don't replace or delete it.

### Task
A boolean expression returns `1` (true) or `0` (false).

**Your task:** In `task.sql`, modify the expression so that instead of checking
whether `?` is greater than zero, it checks whether `?` is **greater than or equal to
`1.1` and less than or equal to the square root of 122**.

Edit `task.sql` and click **Check**.
